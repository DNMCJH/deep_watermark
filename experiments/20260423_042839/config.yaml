attack_prob: 0.8
attacks:
- gaussian_noise
- jpeg_compression
- gaussian_blur
- resize
- crop
batch_size: 16
blur_kernel_size: 5
crop_ratio: 0.8
experiment_dir: experiments
grad_clip_norm: 1.0
image_size: 256
jpeg_quality: 50
lambda_watermark: 5.0
learning_rate: 0.001
log_interval: 50
noise_std: 0.02
num_epochs: 100
num_workers: 4
residual_scale: 0.1
resize_ratio: 0.7
save_interval: 10
scheduler: cosine
seed: 42
train_dir: dataset/train
val_dir: dataset/val
warmup_epochs: 5
watermark_length: 32
